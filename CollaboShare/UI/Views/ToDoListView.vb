Namespace UI.Views
    Public Class ToDoListView
        Public Profile As Housemate
        Public Sub New(profile As Housemate)
            Me.Profile = profile
            ' This call is required by the designer.
            InitializeComponent()

            ' Add any initialization after the InitializeComponent() call.

        End Sub
    End Class
End NameSpace